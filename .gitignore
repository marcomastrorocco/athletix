node_modules
.next
out
build
.env*.local
.blob-token.local
.DS_Store
*.tsbuildinfo
next-env.d.ts
.vscode
Thumbs.db

# Legacy static-HTML version of the site, kept on disk for reference only
javascript/

# Standalone duplicate of public/appconcept/, kept on disk for reference only
/appconcept/

# Local editor/tooling config
.claude/

# One-off SEO scrape dump (intermediate; regenerated by scripts/extract-old-seo.mjs)
seo-export-raw.json
seo-migration-report.html

# Private runtime content — never commit (PII / secrets / logs)
data/leads.json
data/admin.json
data/activity.json
